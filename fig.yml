web:
  build: .
#  command: python manage.py runserver 0.0.0.0:8000 --settings=django_async.settings_prod
  command: uwsgi uwsgi.ini
  ports:
    - "9090:9090"
    - "9091:9091"
  links:
    - db
db:
  image: postgres
  ports:
    # we map this port to be able to use postgres from outside docker (fron host machine)
    - "5432:5432"
